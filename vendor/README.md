# Platform RC inputs

Run `corepack pnpm prepare:platform` to place the exact hash-verified pre-release platform tarballs
in this directory. `*.tgz` files are ignored and must not be committed or redistributed from this
repository. See `platform-rc.json` for the immutable filenames, sizes, digests, release tag, and
source commit.
